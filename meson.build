project('Reusable Abstract Interfaces in C',
	'c',
	meson_version: '>=0.58.0',
	version: '1.0'
)

c_virtual_device_intf_dep = declare_dependency(
	include_directories: include_directories('virtual_devices', is_system: true)
)
